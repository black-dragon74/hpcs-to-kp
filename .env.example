# Log level: DEBUG, INFO, WARN, ERROR, FATAL
#LOG_LEVEL=INFO

# JSON output file tracking volume state across phases (default: rbd_volumes.json)
#DATA_FILE=rbd_volumes.json

# Kubernetes namespace where ODF is deployed (default: openshift-storage)
# This is used to find and exec into the toolbox pod
#NS=openshift-storage

# StorageClass used by encrypted RBD volumes (default: ocs-storagecluster-ceph-rbd-encrypted)
# Pool, pool ID, cluster ID, and volume name prefix are derived automatically from this.
#STORAGE_CLASS=ocs-storagecluster-ceph-rbd-encrypted

#---------------------------------------------------#
# The values below are not optional and must be set #
#---------------------------------------------------#

# --- HPCS (source KMS) ---
# Used in phase2 to unwrap DEKs from the current HPCS root key.
HPCS_INSTANCE_ID=
HPCS_IAM_API_KEY=
HPCS_KEY_ID=
HPCS_URL=

# --- Key Protect (destination KMS) ---
# Used in phase3 to re-wrap DEKs with the new KP root key.
KP_INSTANCE_ID=
KP_IAM_API_KEY=
KP_KEY_ID=
KP_URL=
